import type { Controls } from './types'

export const DEFAULT_CONTROLS: Controls = {
  jump: 'Space',
  moveLeft: 'ArrowLeft',
  moveRight: 'ArrowRight',
  moveUp: 'ArrowUp',
  moveDown: 'ArrowDown',
}