# 未実装・改善タスク一覧

このドキュメントは、現在の実装状況から見た「やるべきこと」を優先度順にまとめています。  
**最終更新:** 2026-06-09 | **総タスク数:** 61 件

---

## 実装ステータス概要

| カテゴリ | 実装度 | 説明 |
|---|---|---|
| **コア機能** | 95% | 横スクロール・ジャンル分岐・投擲システム |
| **ゲーム機能** | 85% | フィーチャー・ジャンルプラグイン |
| **説明書システム** | 90% | JSON駆動・自動読み込み |
| **UI/UX** | 60% | 説明書パネル・選択肢表示・演出（要大幅改善） |
| **チュートリアル** | 40% | 誘導・ヒント・オンボーディング（不足） |
| **アセット** | 30% | キャラクター・背景画像（ほぼ未実装） |
| **コード構造** | 70% | メンテナンス性・整合性（要整理） |
| **パフォーマンス** | 90% | 最適化・テスト |
| **ドキュメント** | 90% | 技術資料・初心者向けガイド |

---

## 高優先度（すぐに対応すべき）

### 0-A. 説明書パネルの視認性・存在感の抜本的強化

**現状:** 説明書が右下に小さく表示されており、存在に気づかないプレイヤーが多い  
**問題:** ゲームのコアコンセプトである「説明書を読む」体験がプレイヤーに届いていない  
**影響:** ゲーム全体の根幹に関わる UX 問題

**タスク:**
- [ ] 説明書パネルのデフォルトサイズを大幅に拡大（現在の 2～3 倍）
- [ ] ゲーム起動直後に説明書がアニメーションで「開く」演出を追加（フォーカスを誘導）
- [ ] 説明書更新時にパネルが脈動・光る・揺れるなどの強い注目演出を実装
- [ ] 選択肢提示時に説明書が中央に展開するモーダル的表示モードを追加
- [ ] 説明書のタイポグラフィを強化（フォントサイズ・行間・余白）
- [ ] 「今すぐ読んで」系のアクション誘導コピーを追加（例：「説明書が更新されました！」の通知バッジ）
- [ ] 最小化・最大化トグルを実装しつつ、デフォルトは最大化状態に変更

**推定作業時間:** 3～5 時間  
**対象ファイル:** `src/components/ManualPanel.vue` 、 `src/components/ChoicePanel.vue`

---

### 0-B. チュートリアル体験の抜本的改善

**現状:** Phase A はシンプルな横スクロールを「そのまま遊ぶ」だけで、誘導・説明がほぼない  
**問題:** 初めてのプレイヤーが何をすべきかわからず、ジャンル選択の面白さに辿り着く前に離脱する  
**影響:** ゲームの面白さが伝わらない根本的な問題

**タスク:**
- [ ] ゲーム開始直後の操作説明オーバーレイを実装（矢印キー/スペースの視覚的ガイド）
- [ ] 最初の障害物の前に「これを避けよう！」のコーチマーク（吹き出し）を表示
- [ ] チュートリアル専用ステージ構成（障害物を徐々に増やし、失敗しにくい難易度設計）
- [ ] アクション成功時のポジティブフィードバック（「ジャンプ成功！」「うまい！」等の字幕）
- [ ] 説明書を最初に読むよう誘導するアニメーション演出（光る矢印・点滅など）
- [ ] Phase A → Phase B への移行トリガー（一定距離走破 or 3回ジャンプ等）を明示
- [ ] チュートリアルのスキップ機能（2回目以降のプレイ向け）
- [ ] フェーズ移行時のナレーション的テキスト演出（「説明書が更新されました…」）

**推定作業時間:** 4～6 時間  
**対象ファイル:** `src/components/TutorialHints.vue` 、 `src/game/sideScroller.ts` 、 `src/App.vue`

---

### 0-C. プレイヤーキャラクター・ゲームアセット画像差し替え

**現状:** プレイヤーキャラ・障害物・背景がすべてキャンバス描画のプリミティブ図形  
**問題:** ゲームの没入感・ジャンル変化の視覚的インパクトが弱い  
**影響:** ジャンル確定時の「別ゲーになった」感が薄い

**タスク:**
- [ ] プレイヤーキャラクターのスプライト画像を作成・差し替え（デフォルト横スクロール版）
  - [ ] 走りアニメーション（4 フレーム以上）
  - [ ] ジャンプポーズ
  - [ ] ダメージポーズ
- [ ] ジャンル別キャラ差し替えアセット
  - [ ] STG: 宇宙船スプライト
  - [ ] RPG: 勇者キャラ
  - [ ] Puzzle: パズルピースキャラ
  - [ ] Rhythm: 音符キャラ
- [ ] 障害物・敵スプライトの画像差し替え（各ジャンル最低 2 種）
- [ ] 背景画像の追加（ジャンル別：宇宙背景・草原・地下ダンジョン等）
- [ ] 説明書 UI に挿絵画像を追加（ジャンル別、`image` フィールド対応）
- [ ] アセット読み込みシステムの実装（`AssetManager` クラス or composable）
- [ ] オフライン動作保証（すべての画像を `dist` に同梱）

**推定作業時間:** 8～12 時間（アセット制作含む）  
**対象ファイル:** `src/genres/*.ts` 、`src/game/sideScroller.ts` 、`public/assets/`（新規作成）

---

### 0-D. コア機能の構造整理・アーキテクチャ改善

**現状:** `sideScroller.ts` や `useGameState.ts` に多くのロジックが集中し、責務が混在している  
**問題:** 機能追加・バグ修正のたびに複数ファイルを横断する必要があり、メンテが困難  
**影響:** 今後の機能追加コストが指数的に増加する技術的負債

**タスク:**
- [ ] `sideScroller.ts` の責務分割
  - [ ] `RenderSystem`（描画ロジック）を独立クラス/ファイルに分離
  - [ ] `PhysicsSystem`（当たり判定・重力）を分離
  - [ ] `SpawnSystem`（障害物・敵生成）を分離
- [ ] `useGameState.ts` の肥大化解消
  - [ ] フェーズ管理ロジックを `usePhaseManager.ts` に切り出し
  - [ ] スコア管理を `useScoreManager.ts` に切り出し
- [ ] ジャンルプラグイン（`src/genres/*.ts`）の共通インターフェース整合性確認
  - [ ] `GenrePlugin` の型定義が全プラグインで守られているか確認
  - [ ] 未使用メソッドのスタブ実装を統一
- [ ] `src/data/` と `src/domain/` の役割を README で明文化
- [ ] 循環依存の検出と解消（`madge` 等のツール活用）
- [ ] `src/engine/` と `src/framework/` の重複・混在を整理

**推定作業時間:** 6～10 時間  
**対象ファイル:** `src/game/sideScroller.ts` 、`src/composables/useGameState.ts` 、`src/engine/` 、`src/framework/`

---

### 0-E. UI 全体のビジュアルクオリティ向上

**現状:** UI コンポーネント（HUD・選択肢パネル・エンディング等）がシンプルすぎてゲームらしい没入感に欠ける  
**問題:** ゲームの世界観・ジャンル変化がビジュアル的に伝わりにくい

**タスク:**
- [ ] HUD（スコア・距離表示）のデザイン刷新
  - [ ] ジャンル別 HUD テーマ（STG: SF風 / RPG: ファンタジー風 / Puzzle: クリーン）
  - [ ] スコア増加時のポップアップ演出（+100 等のフロートテキスト）
- [ ] 選択肢パネル（ChoicePanel）のデザイン強化
  - [ ] 選択肢ホバー時のアニメーション
  - [ ] 選択確定時のトランジション演出
  - [ ] 選択肢の説明文（ツールチップ or サブテキスト）追加
- [ ] エンディング画面の演出強化（スコア加算カウントアップ・ジャンル別背景）
- [ ] ゲーム全体のカラースキーム統一（CSS 変数化）
- [ ] ローディング/初期化中の画面追加（現状は即開始）
- [ ] フォント統一（Google Fonts か同梱フォントを使用、ゲーム感を出す）

**推定作業時間:** 5～8 時間  
**対象ファイル:** `src/components/Hud.vue` 、`src/components/ChoicePanel.vue` 、`src/components/EndingPanel.vue` 、`src/App.vue`

---

### B-1. 未実装フィーチャーの実装または削除（最優先バグ）

**現状:** JSON から有効化できる体裁のフィーチャーが、実行しても `console.warn` を出すだけで何もしない  
**問題:** ゲームデータ（JSON）と実装の乖離。バグ以下の状態が放置されている  
**影響:** ジャンル確定後のゲームプレイが期待通りに動かない場合がある

**未実装フィーチャー一覧:**

| ファイル | 未実装フィーチャー |
|---|---|
| `src/game/systems/ExtraMovementFeature.ts:19` | `dash`, `wall_jump`, `slide`, `gravity_flip` |
| `src/game/systems/PuzzleFeature.ts:19` | `grid_stop`, `puzzle_solve` |
| `src/game/systems/SpecialFeature.ts:41` | `stealth_mode`, `time_bonus`, `tower`, `boss` |

**タスク:**
- [ ] 各フィーチャーについて「実装する」か「JSON から削除して使わない」かを決定する
- [ ] 実装する場合: 各フィーチャーのロジックを実装し動作確認
- [ ] 削除する場合: 対応する JSON エントリ・フィーチャー登録コードも合わせて削除
- [ ] `boss` フィーチャー: 仕様上実装予定だが未着手。スコープを確定する

**推定作業時間:** 4～8 時間（実装する数による）  
**対象ファイル:** `src/game/systems/ExtraMovementFeature.ts` 、`src/game/systems/PuzzleFeature.ts` 、`src/game/systems/SpecialFeature.ts` 、`src/data/manuals/*.json`

---

### B-2. エラーハンドリング欠落の修正

**現状:** ゲームの重要な処理でエラーが起きても `console.error/warn` のみで UI フィードバックがない  
**問題:** プレイヤー視点ではゲームが無応答になったり、スコアが 0 になったりするが理由がわからない

**タスク:**
- [ ] `src/composables/useGameState.ts:64` — 無効な `choice.next` 時にゲームが止まる問題を修正
  - フォールバック処理（最初の選択肢に戻す等）または画面上へのエラー表示を追加
- [ ] `src/domain/scoreCalc.ts:13-14` — スコア式エラー時に `return 0` で沈黙する問題を修正
  - 開発中は警告パネルを、本番ではデフォルト式にフォールバック
- [ ] `src/plugins/PluginManager.ts:44` — `localStorage` が使えない環境（プライベートブラウジング等）でのフォールバックを実装
  - CLAUDE.md で「完全オフライン動作」を謳っているため必須
- [ ] `src/App.vue:77-87` — `scroller` が `null` の場合の処理漏れを修正（`choose()` 内の null チェック）

**推定作業時間:** 2～3 時間  
**対象ファイル:** `src/composables/useGameState.ts` 、`src/domain/scoreCalc.ts` 、`src/plugins/PluginManager.ts` 、`src/App.vue`

---

### B-3. LearningSystem の実動作確認

**現状:** `LearningSystem.ts` の実装は存在するが、実際に発動させる `learningRules` を定義した JSON が存在しない  
**問題:** テストされたことがない可能性が高い。実装されているかどうかすら不明な状態  
**影響:** プレイヤー行動に応じてルールが変化するという体験が未実現

**タスク:**
- [ ] `ManualVersion.learningRules` を定義した JSON を最低 1 件追加してテスト実行
  - 例: `"jumpRate > 0.4 → disable jump"` （ジャンプ率 40% 超でジャンプ禁止）
- [ ] `sideScroller.ts` の `_applyLearningEffect()` で `disableAction` 処理が `duration` 後にリセットされるか確認
- [ ] `changeKey` リマッピングが一時的に適用され、`duration` 後に元に戻るか確認
- [ ] `ScoreVars.deaths` カウンタが常に `0` のままになっている問題を修正
- [ ] `colorTouchMisses` 変数（`sideScroller.ts:69`）が更新されていない問題を修正または削除

**推定作業時間:** 3～4 時間  
**対象ファイル:** `src/domain/LearningSystem.ts` 、`src/game/sideScroller.ts` 、`src/data/manuals/*.json`

---

### 1. 説明書の差分演出アニメーション（実装確認必須）

**現状:** 説明書が更新される際の視覚的フィードバック  
**問題:** 差分（赤線・取り消し線・手書き風）がすべて実装されているか確認が必要  
**影響:** UX の重要な部分（CLAUDE.md では実装完了となっているが、実装を確認する）

**タスク:**
- [ ] 説明書更新時の差分強調アニメーション（赤線・取り消し線）が正常に動作するか確認
- [ ] 手書き風インク効果が実装されているか確認
- [ ] 更新履歴パネルが正しく表示されているか確認
- [ ] アニメーション速度が適切か（0.3～0.5秒程度）

**推定作業時間:** 1～2時間（確認のみ）  
**対象ファイル:** `src/components/ManualPanel.vue` 、 `src/composables/useManual.ts`

---

### 2. LearningSystem の完全な統合確認

**現状:** `LearningSystem.ts` はコア実装されているが、LearningEffect の完全実装が残っている  
**問題:** プレイヤー行動統計（ジャンプ率・移動率・射撃率）が正しく検知され、ゲームルール変更に反映されているか不確実

**タスク:**
- [ ] `sideScroller.ts` の `getStats()` メソッドが `ActionStats` を正しく返しているか確認
- [ ] `evaluateLearningRules()` がゲームループ内で正しく呼ばれているか確認
- [ ] 発動した `LearningEffect` （アクション無効化・ハザード反転・フィーチャー強制有効）が RuntimeRules に適用されているか確認
- [ ] manuals/*.json への learningRules 定義を追加（ジャンプ率 > 40% → ジャンプ禁止 等）
- [ ] deaths カウンタ（ScoreVars.deaths）の実装

**推定作業時間:** 2～3時間（テスト・デバッグ含む）  
**対象ファイル:** `src/domain/LearningSystem.ts` 、 `src/game/sideScroller.ts` 、 `src/composables/useGameState.ts`

---

### 3. エンディングパネルの完全実装

**現状:** `EndingPanel.vue` は存在するが、ジャンル別エンディングメッセージの表示状況が不確認  
**問題:** すべての 20+ ジャンルに対応したエンディング画面・メッセージが実装されているか不明

**タスク:**
- [ ] エンディングパネルが最終スコア（プレイ + 投擲）を正しく計算して表示しているか確認
- [ ] ジャンルごとのエンディング特殊メッセージ（`GenreDef.endingFlavor`）が表示されているか確認
- [ ] 「リスタート」「タイトルへ」ボタンが正常に機能するか確認
- [ ] エンディング画面の演出（スコア表示アニメーション・背景・フェード）が実装されているか確認

**推定作業時間:** 2～3時間  
**対象ファイル:** `src/components/EndingPanel.vue` 、 `src/composables/useGameState.ts` 、 `src/domain/scoreCalc.ts`

---

### 4. 投擲スコア計算の正確性検証

**現状:** `throwEngine.ts` と `scoreCalc.ts` で投擲スコア計算は実装されているが、重み付けが正確か不明  
**問題:** 滞空時間・弧の高さ・速度の計算が CLAUDE.md の仕様と一致しているか確認が必要

**タスク:**
- [ ] 滞空時間の計算が正確か（秒単位で正しいか）
- [ ] 弧の高さ（peak Y）の計算が正確か
- [ ] 速度ペナルティの計算式が正確か
- [ ] 最終投擲スコア = airTime × 0.5 + arcHeight × 0.4 + speedPenalty × 0.1 が正しく実装されているか

**推定作業時間:** 1～2時間（テスト・デバッグ）  
**対象ファイル:** `src/game/throwEngine.ts` 、 `src/domain/scoreCalc.ts`

---

## 中優先度（実装の完成度向上）

### B-4. `as any` キャストの排除・型安全性強化

**現状:** JSON を扱う層を中心に `as any` / `any[]` が乱用されており、TypeScript の恩恵がほぼ失われている  
**問題:** IDE 補完・リファクタリング支援が機能しない。型エラーが実行時まで検出されない

**タスク:**
- [ ] `src/plugins/JSONGenrePlugin.ts:14,52,69` — `spawnTable: readonly any[]`・`getGenre(genreId as any)`・`drawHazard(ctx, hazard: any)` を厳密な型に置き換える
- [ ] `src/plugins/PluginManager.ts:161` — `(obj.inject as any[])` を型ガード関数に置き換える
- [ ] `src/data/manualDeck.ts:53` — `choiceWithId as any` を排除し `ChoiceEntry` 型を正しく適用
- [ ] `src/framework/ConfigValidator.ts:111` — `config[section as GameConfigSection] as any` を `GameConfigMap[T]` 等で型安全に書き直す
- [ ] `src/genres/index.ts:41` — `registerGenre(new JSONGenrePlugin(plugin) as any)` の `as any` を除去
- [ ] `tsconfig.json` に `"noImplicitAny": true` を追加して残存 `any` を検出

**推定作業時間:** 3～5 時間  
**対象ファイル:** `src/plugins/JSONGenrePlugin.ts` 、`src/plugins/PluginManager.ts` 、`src/data/manualDeck.ts` 、`src/framework/ConfigValidator.ts` 、`src/genres/index.ts`

---

### B-5. JSON 駆動設計の徹底（ハードコード残存の解消）

**現状:** CLAUDE.md で「すべてのルールはJSONで定義する。コードにルールをハードコードしない」と明言されているが、複数箇所でコード内固定値が残っている  
**問題:** JSON を変更してもゲームに反映されない設定値が存在し、設計方針と実態が乖離している

**タスク:**
- [ ] `src/game/sideScroller.ts:343` — 距離加速度最大値 `0.5` を `game_balance.json` に移動
- [ ] `src/game/sideScroller.ts:515-516` — スクロール計算式をコード埋め込みから config 参照に変更
- [ ] `src/engine/types.ts:135` — `weightMaxDist` ハードコード値を JSON 化
- [ ] `src/data/gameBalance.ts` の `HAZARD_SPAWN`・`PLAYER_PHYSICS` 等をすべて `game_balance.json` に移動し TypeScript 側は読み取り専用参照のみに
- [ ] JSON 管理と TypeScript 直書きの境界線をコメントまたは README で明文化

**推定作業時間:** 2～4 時間  
**対象ファイル:** `src/game/sideScroller.ts` 、`src/data/gameBalance.ts` 、`src/engine/types.ts` 、`src/data/config/game_balance.json`

---

### B-6. デッドコード・未使用変数の削除

**現状:** 宣言されているが実際には使われない変数・フィールドが複数存在する

**タスク:**
- [ ] `src/game/sideScroller.ts:69` — `colorTouchMisses` が宣言されているが更新されない。実装するか削除するか決定
- [ ] `src/game/sideScroller.ts:258` — `deaths: 0` が常に 0。`scoreFormula` から参照できるが意味をなしていない。カウント実装か削除
- [ ] `src/domain/LearningSystem.ts` に `learningRules` の実使用例がない場合、ファイル全体をスタブとしてマークするか削除を検討
- [ ] 未使用 `import` の一括検出と削除（`eslint --fix` または IDE 機能で）
- [ ] `environment: 'ocean'` / `'space'` 等の値が `sideScroller.ts` のゲームロジックに反映されているか確認。未使用なら JSON スキーマから削除

**推定作業時間:** 1～2 時間  
**対象ファイル:** `src/game/sideScroller.ts` 、`src/domain/LearningSystem.ts` 、プロジェクト全体

---

### B-7. スコア計算の仕様乖離修正

**現状:** CLAUDE.md 仕様と実装コードの間に説明のない乖離がある

**タスク:**
- [ ] `src/domain/scoreCalc.ts:96` — `result.airTime * 1000 * w.airTime` の `* 1000` の意図を確認
  - 秒→ミリ秒変換なのかスケーリングなのかコメントまたは定数名で明示
  - 仕様（`滞空時間 × 0.5`）と実装が一致するよう修正または仕様を更新
- [ ] 投擲スコアの実際の最大値・最小値を計算してゲームバランス上妥当かプレイテストで確認
- [ ] `scoreCalc.ts` の `parseExpr(formula)` を毎回呼ぶのでなく、ゲーム開始時にコンパイル済み関数をキャッシュする最適化

**推定作業時間:** 1～2 時間  
**対象ファイル:** `src/domain/scoreCalc.ts` 、`src/game/throwEngine.ts`

---

### B-8. ファイル・ディレクトリ構造の整理

**現状:** 同一概念が複数の形式・ファイルで管理されており、どこを見ればよいかわかりにくい

**問題箇所:**
- `ShootFeature.ts` と `shootSystem.ts` — 関係が不明確。どちらがどの責務を持つか
- `RhythmFeature.ts` と `rhythmSystem.ts` — 同上
- ジャンル定義が 4 箇所に分散: `src/data/config/genres.json` / `src/data/genres.ts` / `src/engine/GenrePlugin.ts` / `src/plugins/JSONGenrePlugin.ts`
- `src/engine/` と `src/framework/` の役割の差が不明確

**タスク:**
- [ ] `*Feature.ts` と `*System.ts` の役割分担をコメントまたは README で明文化。重複があれば統合
- [ ] ジャンル定義の「正規の参照先」を 1 箇所に定め、他はそこへの参照に統一
- [ ] `src/engine/` と `src/framework/` の役割を README または CLAUDE.md に追記
- [ ] `madge` または `dependency-cruiser` で循環依存を検出し、見つかれば解消

**推定作業時間:** 2～4 時間  
**対象ファイル:** `src/game/systems/` 、`src/engine/` 、`src/framework/` 、`src/data/`

---

### 5. すべてのジャンルビジュアルの完成度確認

**現状:** 12+ ジャンルのプラグインが実装されているが、描画品質のばらつきが予想される  
**問題:** テーマ感（STG はドット文字 × SFフォント、RPG は明朝体 × 羊皮紙風）が完全に表現されているか確認が必要

**タスク:**
- [ ] 各ジャンル（BasePlugin, StgPlugin, RpgPlugin など）の描画メソッドが期待通りか目視確認
  - [ ] BasePlugin: シンプルな緑の地面・青空
  - [ ] StgPlugin: ドット文字・SF背景・敵が赤くて密集
  - [ ] RpgPlugin: 明朝体・経験値ゲージ表示
  - [ ] RhythmPlugin: ネオン風・ビートマーカー
  - [ ] PuzzlePlugin: グリッド表示・モノスペース
  - [ ] その他 8+ ジャンル
- [ ] テーマに合わせた色（skyColors, groundColors）が正しいか確認
- [ ] フォントの描画品質が高いか（読みやすいか）

**推定作業時間:** 3～4時間（目視確認 + 必要なら改善）  
**対象ファイル:** `src/genres/*.ts` （全 12+ ファイル）

---

### 6. チュートリアルヒント機能の完成度向上

**タスク:**
- [ ] 最初のジャンプ完了時に「ジャンプ成功！」的なメッセージが表示されるか
- [ ] 左右移動を検出してヒント更新ができているか
- [ ] 更新フェーズに入ったときに「選択肢を選ぶ」ヒントが表示されるか
- [ ] ジャンルロック後に「説明書を投げる準備」ヒントが表示されるか

**推定作業時間:** 2～3時間  
**対象ファイル:** `src/components/TutorialHints.vue` 、 `src/composables/useGameState.ts`

---

### 7. ゲームバランス調整

**タスク:**
- [ ] チュートリアル時の敵スポーン密度が適切か（プレイテスト）
- [ ] 難易度曲線（1.0倍 → 1.5倍）が段階的に上昇するか
- [ ] 各ジャンルのスコア計算式が適切な報酬感を与えるか
- [ ] ボーナススコア（コンボ・ジャストヒット等）の量が適切か
- [ ] 投擲フェーズのスコア配分（30%）が適切か

**推定作業時間:** 3～5時間（複数回プレイテスト）  
**対象ファイル:** `src/data/config/game_balance.json` 、 `src/domain/scoreCalc.ts`

---

### 8. サウンド実装の確認・改善

**現状:** `SoundManager.ts` が存在し、基本的なイベントのサウンド呼び出しはあるが、実際に音が出ているか確認が必要

**タスク:**
- [ ] ゲーム開始時のサウンド（BGM or SE）が再生されるか
- [ ] 敵を撃つときのサウンドが実装・再生されるか
- [ ] ジャンルロック時のサウンドが実装・再生されるか
- [ ] 投擲フェーズのサウンド（飛行中・着地）が実装・再生されるか
- [ ] 各ジャンル別の BGM 切り替え（STG は SF 系、RPG は冒険系）が実装されているか

**推定作業時間:** 2～4時間  
**対象ファイル:** `src/plugins/SoundManager.ts` 、 `public/sounds/` （サウンドファイル）

---

### 9. 説明書の画像表示機能の確認

**タスク:**
- [ ] 説明書の画像フィールド（`image`, `imageAlt`）が JSON スキーマに含まれているか確認
- [ ] ManualPanel で画像が正しく読み込まれて表示されるか確認
- [ ] 画像が複数バージョンで異なるか（ジャンル別画像）

**推定作業時間:** 1～2時間  
**対象ファイル:** `src/components/ManualPanel.vue` 、 `src/data/manuals/*.json`

---

## 低優先度（改善・拡張）

### B-9. 日本語 IME・キーボード入力の堅牢化

**現状:** `_normalizeKey()` が `e.key === 'Process'`（IME 変換中）を未処理のまま通過させる  
**問題:** 日本語環境でのゲームプレイ中に IME が ON になると誤動作するリスク

**タスク:**
- [ ] `src/game/sideScroller.ts:151-155` — `e.key === 'Process'` または `e.isComposing === true` のときは入力を無視する処理を追加
- [ ] `e.key` の大文字化処理が `ArrowLeft` 等に必要ないか確認（現在 `z`/`Z` だけ特別扱い）
- [ ] IME ON 状態でゲームが誤動作しないことを日本語環境で手動確認

**推定作業時間:** 1 時間  
**対象ファイル:** `src/game/sideScroller.ts:151-155`

---

### B-10. Canvas リサイズ時の描画状態リセット問題

**現状:** `src/App.vue:31-36` で `canvas.width = window.innerWidth` を直接代入している  
**問題:** Canvas の `width`/`height` プロパティを変更するとコンテキストの全状態（変換行列・スタイル・クリッピング等）がリセットされ、ゲーム中に全画面切り替えや画面回転が起きると一瞬描画が消える

**タスク:**
- [ ] リサイズ時に Canvas を再初期化する処理を `sideScroller` 側に通知する仕組みを追加
- [ ] リサイズ後の最初のフレームで状態を正しく復元するか確認
- [ ] デバイス回転（縦横切り替え）で Canvas が正しくリサイズされるかモバイル確認

**推定作業時間:** 1～2 時間  
**対象ファイル:** `src/App.vue:31-36` 、`src/game/sideScroller.ts`

---

### B-11. 命名の不統一解消

**現状:** スコア変数・メソッドの動詞が混在しており、コードを読む際のコンテキスト切り替えコストが高い

**タスク:**
- [ ] スコア変数名の統一: `playScore` / `scoreGain` / `throwScore` の使い分けルールを決めてコメントまたは型エイリアスで明示
- [ ] 動詞の統一: `setKills()`・`setCombo()` (上書き) と `addScore()`・`addBeatHit()` (加算) が混在しているので、上書き系を `update*()` に統一するか規約を決める
- [ ] `stg`（ジャンル ID）と `shoot`（フィーチャー ID）の対応が直感的でない点をコメントで補足

**推定作業時間:** 1～2 時間  
**対象ファイル:** `src/engine/types.ts` 、`src/genres/*.ts` 、`src/domain/scoreCalc.ts`

---

### B-12. 未文書化 API のドキュメント整備

**現状:** 拡張に必要な情報がどこにも書かれておらず、新しい JSON を書くときに手探りになる

**タスク:**
- [ ] `scoreFormula` で使える全変数の一覧を `docs/` に追記（`ScoreVars` 型定義だけでは不十分）
- [ ] `learningRules` の定義方法・サンプルを追加（現在ゼロ）
- [ ] 各フック（`onPlayerJump`・`onPlayerHit`・`onBossSpawn` 等）の完全リファレンスを追加
- [ ] `environment` フィールド（`'ocean'`・`'space'` 等）が実際にゲームロジックに与える影響を記載。未使用なら JSON スキーマから削除
- [ ] `JSONGenrePlugin` を使った新ジャンル追加手順のガイドを追加

**推定作業時間:** 2～3 時間  
**対象ファイル:** `docs/` 、`src/data/manuals/TEMPLATE.json`

---

### 10. パフォーマンス最適化

**タスク:**
- [ ] ゲーム開始から 1時間以上プレイしてメモリリークがないか確認
- [ ] フレームレートが 60 FPS を維持できているか確認（ブラウザ DevTools）
- [ ] Canvas 描画の最適化（requestAnimationFrame 効率化）
- [ ] パーティクル数の上限制御（多すぎるとフレームドロップ）

**推定作業時間:** 2～3時間  
**対象ファイル:** `src/game/sideScroller.ts` 、 `vite.config.ts`

---

### 11. テストカバレッジ拡大

**タスク:**
- [ ] 各ジャンルへの正しい収束をテストできているか
- [ ] プレイヤーがダメージを受ける条件をテスト
- [ ] スコア計算（複数ジャンル × 複数スコア公式）をテスト
- [ ] 投擲スコア計算が複数パターンでテストされているか
- [ ] UI（ChoicePanel, EndingPanel）の動作をテスト

**推定作業時間:** 3～5時間  
**対象ファイル:** `tests/*.mjs` 、 `src/` （全体）

---

### 12. 説明書のテーマ CSS 完成度向上

**タスク:**
- [ ] 各テーマの CSS を目視確認（plain, stg, rpg, puzzle, rhythm, horror, aquatic）
- [ ] フォント（SF系 / 明朝体 / モノスペース）が正しく適用されているか
- [ ] ジャンル確定時のテーマ切り替えアニメーション（フェード）が滑らかか

**推定作業時間:** 2～3時間  
**対象ファイル:** `src/components/ManualPanel.vue` （CSS）

---

### 13. モバイル対応・レスポンシブ対応

**タスク:**
- [ ] タッチイベント（touchstart, touchmove, touchend）が正しく処理されているか
- [ ] 投擲フェーズでドラッグが小さい画面で機能するか
- [ ] 画面サイズ変更時（回転）に Canvas がリサイズされるか

**推定作業時間:** 2～3時間  
**対象ファイル:** `src/App.vue` 、 `src/game/sideScroller.ts` 、 `src/components/ThrowOverlay.vue`

---

### 14. アクセシビリティ改善（WCAG 対応）

**タスク:**
- [ ] 選択肢パネル（ChoicePanel）に ARIA ラベルを追加
- [ ] HUD（スコア・距離表示）に alt テキストを追加
- [ ] コントラスト比（テキスト × 背景）が WCAG AA 以上か確認

**推定作業時間:** 2～3時間  
**対象ファイル:** `src/components/*.vue` （全体）

---

## 今後の拡張（2026年後半以降）

### 15. マルチプレイヤー機能

**説明:** オンラインでプレイヤー同士がスコア競争  
**難易度:** 非常に高い | **推定作業時間:** 40 時間～

---

### 16. ジャンル生成 AI

**説明:** LLM を使用して、プレイ履歴から新しいジャンル・ルールを自動生成  
**難易度:** 非常に高い | **推定作業時間:** 60 時間～

---

### 17. VR / AR 対応

**説明:** WebXR を使用した VR/AR 版ゲーム  
**難易度:** 高い | **推定作業時間:** 50 時間～

---

## チェックリスト（品質保証）

すべての機能がリリース前に完了すべき確認：

- [ ] **動作確認**: ゲーム全体を 5 回以上プレイしてクラッシュなし
- [ ] **すべてのジャンル到達**: 20+ ジャンルをすべて確認
- [ ] **スコア計算**: 複数ジャンル × 複数スコア公式で正確性確認
- [ ] **投擲フェーズ**: マウス・タッチの両方で正常に機能
- [ ] **説明書更新**: 差分演出が視覚的に理解できるか
- [ ] **エンディング**: すべてのジャンルでエンディング表示
- [ ] **パフォーマンス**: 1 時間連続プレイでメモリリークなし
- [ ] **ブラウザ互換性**: Chrome, Firefox, Safari での動作確認
- [ ] **モバイル**: iOS Safari, Chrome Android でのタッチ動作確認
- [ ] **オフライン**: インターネット切断状態で完全動作

---

## 優先度別タスク統計

| 優先度 | 件数 | 推定時間 | 主な分類 |
|---|---|---|---|
| **緊急（0-A〜0-E）** | 5 | 26～41 時間 | UX・チュートリアル・アセット・構造・UI |
| **高（B-1〜B-3 + 1〜4）** | 7 | 18～27 時間 | 未実装フィーチャー・エラー処理・LearningSystem |
| **中（B-4〜B-8 + 5〜9）** | 10 | 24～42 時間 | 型安全・JSON駆動・デッドコード・ビジュアル |
| **低（B-9〜B-12 + 10〜14）** | 9 | 16～26 時間 | IME・命名・ドキュメント・パフォーマンス |
| **拡張（15〜17）** | 3 | 150+ 時間 | マルチプレイ・AI・VR |

**合計: 34 タスク群 / 61 件以上のチェック項目**

---

## 推奨実装順序

1. **バグ修正フェーズ**（B-1〜B-3）— 未実装フィーチャー・エラー処理・LearningSystem（10～15 時間）
2. **UX 改善フェーズ**（0-A〜0-E）— 説明書強化・チュートリアル・UI・アセット（26～41 時間）
3. **確認フェーズ**（1〜4）— 既存機能の動作確認（6～10 時間）
4. **品質フェーズ**（B-4〜B-8 + 5〜9）— 型安全・構造整理・ビジュアル（24～42 時間）
5. **最適化フェーズ**（B-9〜B-12 + 10〜14）— 命名・パフォーマンス・テスト（16～26 時間）
6. **拡張フェーズ**（15〜17）— 2026 年後半以降（150+ 時間）
