diff --git a/src/genres/BasePlugin.ts b/src/genres/BasePlugin.ts
index e11381f..4d5bc1e 100644
--- a/src/genres/BasePlugin.ts
+++ b/src/genres/BasePlugin.ts
@@ -108,17 +108,6 @@ export abstract class DarkThemePlugin extends GenrePluginBase {
     ctx.stroke()
   }
 
-  protected _roundRect(ctx: CanvasRenderingContext2D, x: number, y: number, w: number, h: number, r: number): void {
-    ctx.beginPath()
-    ctx.moveTo(x + r, y); ctx.lineTo(x + w - r, y)
-    ctx.quadraticCurveTo(x + w, y, x + w, y + r)
-    ctx.lineTo(x + w, y + h - r)
-    ctx.quadraticCurveTo(x + w, y + h, x + w - r, y + h)
-    ctx.lineTo(x + r, y + h)
-    ctx.quadraticCurveTo(x, y + h, x, y + h - r)
-    ctx.lineTo(x, y + r); ctx.quadraticCurveTo(x, y, x + r, y)
-    ctx.closePath()
-  }
 }
 
 // 笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏
